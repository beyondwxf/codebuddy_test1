<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taskmanager.mapper.SysDeptMapper">

    <resultMap id="DeptResultMap" type="com.taskmanager.domain.SysDept">
        <id property="deptId" column="dept_id"/>
        <result property="parentId" column="parent_id"/>
        <result property="ancestors" column="ancestors"/>
        <result property="deptName" column="dept_name"/>
        <result property="orderNum" column="order_num"/>
        <result property="leader" column="leader"/>
        <result property="phone" column="phone"/>
        <result property="email" column="email"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <!-- 查询全部部门树 -->
    <select id="selectDeptTreeAll" resultMap="DeptResultMap">
        SELECT * FROM sys_dept
        WHERE del_flag = '0'
        ORDER BY parent_id, order_num ASC
    </select>

    <!-- 根据部门ID查询子部门数量（删除前校验） -->
    <select id="selectChildrenCountByDeptId" resultType="int">
        SELECT COUNT(*) FROM sys_dept
        WHERE parent_id = #{deptId} AND del_flag = '0'
    </select>
</mapper>

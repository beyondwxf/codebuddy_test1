<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taskmanager.mapper.WarehouseMapper">

    <!-- 仓库结果映射 -->
    <resultMap id="WarehouseResultMap" type="com.taskmanager.domain.Warehouse">
        <id property="warehouseId" column="warehouse_id"/>
        <result property="warehouseName" column="warehouse_name"/>
        <result property="warehouseCode" column="warehouse_code"/>
        <result property="province" column="province"/>
        <result property="address" column="address"/>
        <result property="warehouseType" column="warehouse_type"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <!-- 分页查询仓库列表 -->
    <select id="selectWarehouseList" resultMap="WarehouseResultMap">
        SELECT * FROM wms_warehouse
        WHERE del_flag = '0'
        <if test="warehouseName != null and warehouseName != ''">
            AND warehouse_name LIKE CONCAT('%', #{warehouseName}, '%')
        </if>
        <if test="warehouseCode != null and warehouseCode != ''">
            AND warehouse_code LIKE CONCAT('%', #{warehouseCode}, '%')
        </if>
        <if test="provinceList != null and provinceList.size() > 0">
            AND province IN
            <foreach collection="provinceList" item="item" open="(" separator="," close=")">
                #{item}
            </foreach>
        </if>
        <if test="warehouseType != null and warehouseType != ''">
            AND warehouse_type = #{warehouseType}
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 查询所有正常状态的仓库 -->
    <select id="selectWarehouseAll" resultMap="WarehouseResultMap">
        SELECT * FROM wms_warehouse
        WHERE del_flag = '0' AND status = '0'
        ORDER BY warehouse_name
    </select>

</mapper>

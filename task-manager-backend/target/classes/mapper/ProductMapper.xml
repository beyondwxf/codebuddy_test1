<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.taskmanager.mapper.ProductMapper">

    <!-- 商品结果映射 -->
    <resultMap id="ProductResultMap" type="com.taskmanager.domain.Product">
        <id property="productId" column="product_id"/>
        <result property="productName" column="product_name"/>
        <result property="skuCode" column="sku_code"/>
        <result property="description" column="description"/>
        <result property="previewImage" column="preview_image"/>
        <result property="mobileContent" column="mobile_content"/>
        <result property="salePrice" column="sale_price"/>
        <result property="purchasePrice" column="purchase_price"/>
        <result property="unit" column="unit"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <!-- 分页查询商品列表 -->
    <select id="selectProductList" resultMap="ProductResultMap">
        SELECT * FROM wms_product
        WHERE del_flag = '0'
        <if test="productName != null and productName != ''">
            AND product_name LIKE CONCAT('%', #{productName}, '%')
        </if>
        <if test="skuCode != null and skuCode != ''">
            AND sku_code LIKE CONCAT('%', #{skuCode}, '%')
        </if>
        <if test="status != null and status != ''">
            AND status = #{status}
        </if>
        <if test="minPrice != null">
            AND sale_price &gt;= #{minPrice}
        </if>
        <if test="maxPrice != null">
            AND sale_price &lt;= #{maxPrice}
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 根据商品ID查询商品详情 -->
    <select id="selectProductById" resultMap="ProductResultMap">
        SELECT * FROM wms_product
        WHERE del_flag = '0' AND product_id = #{productId}
    </select>

</mapper>

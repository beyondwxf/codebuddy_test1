# 测试环境配置 - 禁用 Redis 相关自动配置
spring:
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisReactiveAutoConfiguration
  data:
    redis:
      repositories:
        enabled: false
